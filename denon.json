{
  "$schema": "https://deno.land/x/denon/schema.json",

  "scripts": {
    "dev": {
      "cmd": "deno run --import-map=import_map.json server.ts",
      "desc": "Run the main server.",
      "allow": ["env", "net"]
    }
  }
}
